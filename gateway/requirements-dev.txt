# Test-only dependencies, kept separate from requirements.txt so the Stage 7
# container image does not ship a test framework it will never run.
#
# Install:  pip install -r gateway/requirements-dev.txt

-r requirements.txt

pytest==8.3.4
